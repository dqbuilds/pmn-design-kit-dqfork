# Rendering (lib + generators)
numpy>=1.24
Pillow>=10.0

# Data sourcing (skills/market-data/scripts/*.py — Polymarket, Hyperliquid, CoinGecko, DeFiLlama, DexScreener)
requests>=2.28

# Optional — only for skills/market-data/scripts/visuals.py (matplotlib chart/card renderer)
matplotlib>=3.7
